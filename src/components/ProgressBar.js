import React from 'react';\n\nconst ProgressBar = ({ current, total, answeredCount }) => {\n  const progress = Math.floor((answeredCount / total) * 100);\n  \n  return (\n    <div className=\"mb-8\">\n      <div className=\"flex justify-between items-center mb-2\">\n        <span className=\"text-sm font-medium text-blue-600\">{progress}% Selesai</span>\n        <span className=\"text-sm font-medium text-gray-600\">Pertanyaan {current} dari {total}</span>\n      </div>\n      <div className=\"bg-gray-200 rounded-full h-2.5 overflow-hidden\">\n        <div\n          className=\"bg-blue-600 h-2.5 rounded-full animate-progress\"\n          style={{ width: `${progress}%` }}\n        />\n      </div>\n      <div className=\"flex justify-between mt-2\">\n        <span className=\"text-sm text-gray-500\">{answeredCount} pertanyaan dijawab</span>\n        <span className=\"text-sm text-gray-500\">{total - answeredCount} pertanyaan tersisa</span>\n      </div>\n    </div>\n  );\n};\n\nexport default ProgressBar;